version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: saeed_erp_backend
    env_file:
      - .env
    volumes:
      - sqlite_data:/app/data
    expose:
      - "8000"
    restart: unless-stopped

volumes:
  sqlite_data:
